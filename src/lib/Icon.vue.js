;
"$emit('click',$event)";
class {
}
"classes" >
    class {
    };
"icon" >
;
xlink: href = "'#'+IconName" > /use>
    < /svg>
    < /div>
    < /template>
    < script;
lang = "ts" >
;
import { computed } from 'vue';
export default {
    name: 'Icon',
    setup(props) {
        console.log(props);
        let classes = computed(() => {
            return {
                'fish-ui-icon': true
            };
        });
        return { classes };
    }
}
    < /script>
    < style;
lang = "scss";
scoped >
    /style>;
